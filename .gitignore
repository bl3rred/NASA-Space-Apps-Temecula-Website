node_modules/
dist/
.env
.env.*
!.env.example

.DS_Store
Thumbs.db
*.log

.vite/
tsconfig.tsbuildinfo

.cursor/
.freebuff/

# Non-deploy artifacts / local QA
qa/

# Root-only SEO source inputs (we use resized copies in /public)
website share image.png
top image norm.png
top image funny.png

# Optional: local source for the dandelion texture (public/ already has deploy-ready copy).
# Anchored to the repo root — otherwise it also ignores public/assets/particles/dandelion.png.
/dandelion.png
